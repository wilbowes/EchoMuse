services:
  echomuse-controller:
    build: .
    container_name: echomuse-controller
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
